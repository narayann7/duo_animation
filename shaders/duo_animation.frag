#version 460 core

// Placeholder fragment program so pubspec.yaml's shaders declaration
// resolves to a real, compilable file. Task 7 replaces this with the
// tilt-driven frosted-glass fold shader.

precision mediump float;

layout(location = 0) out vec4 fragColor;

void main() {
  fragColor = vec4(1.0, 1.0, 1.0, 1.0);
}
